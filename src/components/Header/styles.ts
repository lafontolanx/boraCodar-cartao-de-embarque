import styled from "styled-components";

export const Container = styled.div`
    padding-bottom: 1.25rem;
    
    h2 {
        font-size: 20px;
        font-weight: 500;
        color: var(--white);
    }
`