import styled from "styled-components";

export const Container = styled.div`
    padding-top: 1.25rem;
    width: 327px;
    text-align: center;

    p {
        font-size: 14px;
        font-weight: 400;
        color: var(--white);
    }
`